# 注意保存为UTF-8格式
spring:
  profiles:
    active:
    - dev
---
spring:
  profiles: dev
  application:
    name: dahe-data-config-dev
---
spring:
  profiles: test
  application:
    name: dahe-data-config-test
 
